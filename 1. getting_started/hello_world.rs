fn main() {
    println!("{}", "Hello World");
    // macro!(...) => extends rust syntax
    //function(...) => good ol' function
}

/*
    NOTE:
    rustfmt => automatic formatter tool 
 */

/*
    compile with:
        rustc [file].rs => generates an executible
*/